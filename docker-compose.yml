version: '3.8'
services:
  app:
    image: python:3.11-slim
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    volumes:
      - ./:/app
    working_dir: /app
    ports:
      - "8000:8000"
